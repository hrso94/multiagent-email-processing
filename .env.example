# Kopiraj ovaj file u .env i upisi svoje vrijednosti.
# .env je u .gitignore - nikad se ne commita.

# mock (default, besplatno, bez API poziva) ili openai (pravi poziv)
LLM_PROVIDER=mock

# Treba samo ako je LLM_PROVIDER=openai
OPENAI_API_KEY=
